import { OwnNgIfDirective } from './own-ng-if.directive';

describe('OwnNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
